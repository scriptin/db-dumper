# Simplistic shell database dumper for MySQL

This script can be used on shared hostings which have no backup utilities, but give you a way to execute bash scripts (maybe via cron).

See usage instructions in the script source.
